input {
  tcp {
    port => 5000
    codec => json_lines 
  }
}


filter {
}


output {
 elasticsearch {
index => "%{[app]}-%{+YYY.MM.dd}"
    hosts=> "${ELASTIC_HOSTS}"
    user=> "${ELASTIC_USER}"
    password=> "${ELASTIC_PASSWORD}"
    cacert=> "/usr/share/logstash/certs/ca/ca.crt"
 }
}
